<div class="lead my-3"><%= sanitize @hotel.title %></div>

<div class="lead my-3"><%= @hotel.stars %></div>
<div class="lead my-3"><%= sanitize @hotel.address %></div>
<div class="lead my-3"><%= sanitize @hotel.link_map %></div>
<div class="lead my-3"><%= sanitize @hotel.pay_one %></div>
<div class="lead my-3"><%= sanitize @hotel.pay_two %></div>
<div class="lead my-3"><%= @hotel.rating %></div>
<div class="lead my-3"><%= sanitize @hotel.about_hotel %></div>
<div class="lead my-3"><%= sanitize @hotel.services %></div>
<div class="lead my-3"><%= sanitize @hotel.extra %></div>
<div class="lead my-3"><%= sanitize @hotel.contacts %></div>


<%= link_to 'Edit', edit_hotel_path(@hotel), class: 'btn btn-secondary' %>
<%= link_to 'Destroy', hotel_path(@hotel), class: 'btn btn-danger', data: {method: :delete, confirm: 'Are you sure?'} %>



<hr class="my-3">

<!--<%= @room.inspect %>-->

<h2>Add room</h2>

<%= render 'shared/errors', object: @room %>

<%= link_to 'Create room', new_hotel_room_path(@hotel), class: 'btn btn-secondary' %>


<h2 class="mt-5">Rooms</h2>

<%= paginate @rooms %>


<%= render partial: 'rooms/room', collection: @rooms, as: :room, locals: {hotel: @hotel} %>

<%= paginate @rooms %>


